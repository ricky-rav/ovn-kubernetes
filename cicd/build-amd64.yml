
stages:
  - build
  - image

include:
  - cicd/build-common.yml

default:
  interruptible: true

build:amd64:
  stage: build
  image: ${image}
  extends:
    - .tags-amd64
    - .ovn-k8s-build-ovn
    - .always

image:amd64:
  image: ${builder_image}
  stage: image
  extends:
    - .tags-amd64
    - .ovn-k8s-build-image
    - .always

